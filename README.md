# Tauri + Solid + Typescript

This template should help get you started developing with Tauri, Solid and Typescript in Vite.

## if using vscode if bug

```bash
export GTK_PATH=""
export GIO_MODULE_DIR=""
npm run tauri dev
```